<% provide(:title, "Messages") %>
<h2>Messages</h2>

<%= link_to 'Post a new message', new_message_path %>

<% @users.each do |c| %>
    <ol class="conversations">
        <li><%= link_to c.username, message_path(c.username) %></li>
    </ol>
<% end %>