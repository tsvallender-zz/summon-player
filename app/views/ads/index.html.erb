<% provide(:title, "Ads") %>
<h2>Ads</h2>

<%= link_to 'Post a new ad', new_ad_path %>

<h3>Filter:</h3>
<%= render 'search' %>

<%= will_paginate %>

<% if @ads.any? %>
    <ol class="ads">
        <%= render @ads %>
    </ol>
    <%= will_paginate @ads %>
<% end %>